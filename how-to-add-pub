pipx seems best to get academic installed. 

pipx install academic
academic import --bibtex file.bib

then add featured.gif to publication folder

might need to adjust index.md to remove the "link" block, because otherwise there are netlify build errors.